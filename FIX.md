Check whether MongoDB persistence works correctly (encryption/decryption), (fetch, update).
Many unhandled exceptions are there. There are empty catch blocks without any logging and the handler code.
Check migration service works correctly.